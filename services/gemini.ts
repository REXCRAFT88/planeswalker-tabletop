// Gemini AI service removed — dead code, API key was being leaked into the frontend bundle.
// If you need an AI Judge feature in the future, implement it as a server-side API route
// in server/index.ts so the API key never reaches the client.
